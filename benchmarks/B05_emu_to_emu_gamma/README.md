# B05: e- mu- -> e- mu- gamma

This is the bounded v0.2 native-QED 2-to-3 feature spike. FeynArts/FeynCalc remains the amplitude authority. The benchmark expects the four external charged-leg bremsstrahlung diagrams and records only structural metadata, never hand-written amplitude gold.

The soft check compares the generated emission coverage with the B03 `e- mu- -> e- mu-` hard process. `compute_m2.wl` is generated as a non-executed policy artifact; phase-space integration and cross sections are out of scope.
