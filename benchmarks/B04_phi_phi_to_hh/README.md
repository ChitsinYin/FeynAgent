# B04 phi phi to h h

B04 registers the locked custom audited gravity benchmark input for `phi(p1) phi(p2) -> h(k1) h(k2)` using `model_id: reheating_scalar_gravity_v1` and `process_id: process:B04_phi_phi_to_h_h` from the external knowledge lock.

This directory contains only sanitized, publishable benchmark registration material: a backend-neutral PhysicsCard, convention reference, process description, expected topology summary, logical rule manifest, and hash-only knowledge manifest.

The local external knowledge root is registered outside Git in `.feynagent/knowledge_roots.yaml`. Do not commit source PDFs, notebooks, raw gold files, generated amplitudes, M2 outputs, or machine-local absolute paths.

Current scope: custom audited intake and backend feasibility only. B04 amplitude construction and M2 calculation are explicitly not started by this registration.
