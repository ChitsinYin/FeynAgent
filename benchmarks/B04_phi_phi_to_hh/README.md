# B04 phi phi to h h

B04 is the single locked custom audited gravity benchmark route for `phi(p1) phi(p2) -> h(k1) h(k2)` using `model_id: reheating_scalar_gravity_v1`, `process_id: process:B04_phi_phi_to_h_h`, and backend `direct_feyncalc_custom_audited`.

This directory contains sanitized, publishable benchmark registration material: a backend-neutral PhysicsCard, convention reference, process description, expected topology summary, logical rule manifest, and hash-only knowledge manifest.

The local external knowledge root is registered outside Git in `.feynagent/knowledge_roots.yaml`. Do not commit source PDFs, notebooks, raw gold files, generated amplitudes, M2 outputs, or machine-local absolute paths.

Current v0.1 scope: locked B04 topology, amplitude, LaTeX, backend dispatch, rule-audit, and skill-route checks are validated only for this benchmark and only with the separately supplied audited external knowledge package. This is not arbitrary gravity or arbitrary BSM support.

B04 topology and amplitude authorization do not authorize B04 M2. Any B04 M2 calculation requires separate explicit operations and remains outside automatic public execution.
